import React from 'react'

const ProfileCard = ({role, experience}) => {
    // This is the child of APP
    console.log("Profile card is child of APP and it is rendered")
  return (
    <div>
      {/* <h1> {role} - {experience} years </h1> */}
    </div>
  )
}

export default ProfileCard
